language: node_js
node_js:
  - "stable"
env:
  global:
    - DEPLOY_HOST=sybolt.com
cache:
  directories:
    - node_modules
script: # dummy script
  - cd .
deploy:
  provider: script
  script: bash .travis/deploy.sh
  skip_cleanup: true # preserve build artifacts
  on:
    branch: travis-tests

